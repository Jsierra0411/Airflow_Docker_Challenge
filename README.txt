

1. tenemos un archivo yaml de docker seteado para correr en local
2. para crear los contenedores de airflow , debemos correr en consola el comando: docker compose up airflow-init
3. con este comando podemos iniciar los containers: docker compose up
4. instalar libreria pandas.